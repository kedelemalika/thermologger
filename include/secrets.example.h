#pragma once

// Salin file ini menjadi `include/secrets.h` lalu isi credential broker.
// File `secrets.h` sengaja di-ignore oleh git.

#define APP_MQTT_USERNAME "your-mqtt-username"
#define APP_MQTT_PASSWORD "your-mqtt-password"
